---
title: 'Blog'
description: 'Welcome to our blog where you can read all the latest news about luna Creates'
heroTitle: 'Our Blog'
heroImage: 'https://cdn.shopify.com/s/files/1/1931/6299/files/homepage.jpg'
layout: _layouts/blog
sitemap:
  changefreq: daily
  priority: 0.8
---

{% set postListItems = collections.postFeed %}

<ol class="dp-g gtc-2 gg-24 md:gtc-3 md:gg-32" reversed>
{% for item in postListItems %}
    {% set title = item.data.title %}
    {% set date = item.data.publish_date or item.date.date %}
    {% set thumbnail = item.data.thumbnail %}
    {% set thumbnailWebP = thumbnail.split(r/(jpg|png)/g)[0] + 'webp' %}
    {% set isDraft = item.data.is_draft %}

    {% if not isDraft %}
    <li>
        <article class="picture-scale pos-r dp-f fd-c">
            <h2 class="fs-sm lh-sm my-8 order-2 md:mt-16">
                <a class="psuedo-link fs-sm lh-sm fvs-md text-body" href="{{item.url}}">{{title}}</a>
            </h2>

            <time class="fs-xs lh-xs fvs-sb text-body order-3" datetime="{{date}}">{{date | readableDate }}</time>

            {% picture
                thumbnail,
                '300, 350',
                '350px',
                null,
                null,
                'ratio-1-1 order-1 ov-hidden -zi-10'
            %}
        </article>
    </li>
    {% endif %}

{% endfor %}
</ol>
